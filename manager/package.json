{
  "name": "personas",
  "version": "0.4.0",
  "description": "Manager for persona advisors. Each persona is installed as its own marketplace plugin; this manager handles enable/disable and routes ask traffic through parley.",
  "license": "MIT",
  "author": {
    "name": "Mischa Sigtermans"
  },
  "repository": "https://github.com/mischasigtermans/claude-personas",
  "type": "module",
  "scripts": {
    "build": "bun build src/server.ts --target=node --format=esm --outfile=dist/server.js",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
